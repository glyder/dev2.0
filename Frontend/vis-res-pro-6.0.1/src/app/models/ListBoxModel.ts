
export class ListBoxModel {

    value: number;
    label: string;
    // selected: number;

    constructor(value: number, label: string) { // , selected: number) {
      this.value = value;
      this.label = label;
      // this.selected = selected;
    }
}
